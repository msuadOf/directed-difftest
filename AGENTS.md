# AGENTS.md — claudefuzz 项目规则（未来 agent 必读）
- 所有的回答、书写的文档都必须使用中文
- 测试策略为TDD（先写测试再写代码）；`make solve`为最终验收功能使用的命令，输出在output/，推荐`rm -rf output/ && make solve-MRET`先清理日志再smoke test，并行参数-j加多少根据运行的主机性能、cpu核心数、已有进程占用合理分配核心数，优先使用用户提供的。
- 不要额外地在未使用的变量前面加下划线
- 修改文件后运行`cargo fmt`保持rust代码格式对齐
- `agents/guides.md`记录了一些代码思路上的方针

## 硬规则

1. **先读 `docs/workflow-detailed.md` 再干活**。所有阶段定义、schema、终止条件以该文档为准；简版只作速览。
2. **中间文件一律放 `artifacts/<疑点id>/round<N>/`**（源码、ELF、编译产物、日志、提交跟踪）。不要写到仓库其他位置或系统 /tmp。
3. **所有结论必须三分类**：真实可触发（bug）/ 被机制天然规避（写明哪条机制）/ 当前配置不可达（写明何时暴露）。不允许二值"有/无 bug"。
4. **复现用例必须留 ELF**：每个"真实可触发"结论对应的最小复现 ELF 不许删除，路径写入汇总的复现清单。
5. **不修改 XiangShan 源码**（`workspace` 下的 difftest-xiangshan/xiangshan 等）。本仓库只读 RTL、只写本仓库文件；也不自动修 RTL、不开 PR。

## 工作约定

- 环境信息（emu/REF/编译器/GOODTRAP/-b/-e）见 `docs/workflow-detailed.md` 的"固定环境信息"一节，直接抄进 prompt，勿自行重建环境。
- Isolate 迭代的终止由 Workflow JS 代码判定（轮数上限 + 连续一轮无新信息），agent 不要自行决定"差不多了"。
- 测试通过 ≠ 无 bug：必须做灵敏度对照和 trap/flushPipe/vsetvli 后的一致性追问。
- Skeptic 的 REFUTED 结论不回环重做，标人工复核。
- Synthesize 的 findings.md 条目只出草稿，不直接改任何共享文件。


# coding
- 用户要求修改语法bug或者补全代码的时候，语法有错误不允许将造成语法问题的那部分代码删去，应该顺着用户已有代码的思路和想法把代码完善；要遵循指令，对代码改动的时候，不要因为需要对现有代码兼容而更改了实现的方式
- 防御性编程，禁止 Fallback。对于内部逻辑错误、不变量违反、不可恢复状态：**直接 `panic!()` / `unwrap()` / `expect()`**，不要试图"优雅"处理。
- 不要过度封装，封装一个函数要反复考虑，多点代码复用（不考虑test代码）才考虑封装
- 在新加代码的时候，需要先思考一下仓库中什么文件适合存放新加的内容，而不是把新增的代码全部堆在几个关注的文件中，多反问自己新代码存放在这个文件适合不适合
